%% -- main sequence -- %%
ALPHA :time:

    day :leap:
    :leap: <- warp(:isLeap:)(:year:)
    start check (:leap: ?= 0)
        %% it's a leap year, print :year: %%
        display :year: %% TODO: i/o syntax %%
    end check

OMEGA :time: